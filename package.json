{
  "name": "mailer-cli",
  "version": "0.1.0",
  "description": "A command-line tool to send e-mails via SMTP",
  "main": "mailer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Salvatore Zappalà (https://salvatorezappala.info)",
  "license": "MIT",
  "dependencies": {
    "nodemailer": "^4.4.1"
  }
}
